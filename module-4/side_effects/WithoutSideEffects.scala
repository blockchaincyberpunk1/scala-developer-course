def calculateSquare(number: Int): Int = {
    number * number
}


// The calculateSquare function calculates the square of a number. 
// It's side effect-free—it doesn't modify any state outside its scope.
